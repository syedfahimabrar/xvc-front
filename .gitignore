__pycache__/
*.pyc
.venv/
uv.lock

# Benchmark artifacts — results belong in docs/BENCHMARKS.md, not as binaries.
bench.json
*.wav

.DS_Store


# Swift build products
.build/

# Driver build: BlackHole is cloned by mac/driver/build.sh, not vendored
mac/driver/BlackHole/
mac/driver/build/
